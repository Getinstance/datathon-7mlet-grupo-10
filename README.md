# special-octo-eureka
FIAP Fase 5

### start a new python env
`python -m venv .venv`

### install the dependencies
`.venv/bin/pip install .`

### executar a validação dos dados iniciais
`./validate.sh`